<form class="{{cssClass}} fantasy-item-sheet" autocomplete="off">
  <div class="oi-frame sty-ornate">
    {{> "systems/stryder/templates/item/parts/ornate-header.hbs" typeLabel="Ingredient"}}

    {{!-- Extra header: Category, Quality, Sell Price --}}
    <div class="oi-extra-header">
      <div class="dropdown-field">
        <label class="resource-label secondary-input">Category</label>
        <select class="secondary-input" name="system.ingredient_type">
          {{#sty-select system.ingredient_type}}
          <option value="base">Base</option>
          <option value="protein">Protein</option>
          <option value="spice">Spice</option>
          <option value="sauce">Sauce</option>
          {{/sty-select}}
        </select>
      </div>

      <div class="dropdown-field">
        <label class="resource-label secondary-input">Quality</label>
        <select class="secondary-input ingr-quality-select" name="system.quality">
          {{#sty-select system.quality}}
          <option value="rotten">Rotten (−2)</option>
          <option value="bad">Bad (−1)</option>
          <option value="good">Good (+0)</option>
          <option value="great">Great (+1)</option>
          <option value="gourmet">Gourmet (+2)</option>
          {{/sty-select}}
        </select>
      </div>

      <div class="dropdown-field">
        <label class="resource-label secondary-input">Sell Price (Grail)</label>
        <input class="tertiary-input" type="number" name="system.sell_price"
          value="{{system.sell_price}}" placeholder="0" data-dtype="Number" />
      </div>
    </div>

    {{!-- Enchanted toggle --}}
    <div class="oi-enchanted-row">
      <input type="checkbox" name="system.is_enchanted" {{#if system.is_enchanted}}checked{{/if}}
        id="ingr-enchanted-{{item.id}}" />
      <label for="ingr-enchanted-{{item.id}}">Enchanted (Monster Component)</label>
    </div>

    {{#if system.is_enchanted}}
    <div class="oi-enchant-type-row">
      <div class="dropdown-field">
        <label class="resource-label secondary-input">Enchant Type</label>
        <select class="secondary-input" name="system.enchant_type">
          {{#sty-select system.enchant_type}}
          <option value="">— None —</option>
          <option value="bones">Bones — Hardy (Rush Party Action)</option>
          <option value="eyes">Eyes — Watchful (Raise Perception)</option>
          <option value="mana_veins">Mana Veins — Restoration (Restore Mana)</option>
          <option value="heart">Heart — Power (Unique Ward)</option>
          {{/sty-select}}
        </select>
      </div>
    </div>
    {{/if}}

    {{!-- Quality modifier callout --}}
    <div class="ingr-quality-bar">
      <span class="ingr-quality-label">Quality Modifier</span>
      <span class="ingr-quality-value {{rarityClass}}">
        {{#if (eq system.quality 'rotten')}}−2 · Rotten
        {{else if (eq system.quality 'bad')}}−1 · Bad
        {{else if (eq system.quality 'good')}}+0 · Good
        {{else if (eq system.quality 'great')}}+1 · Great
        {{else if (eq system.quality 'gourmet')}}+2 · Gourmet
        {{/if}}
      </span>
      <span class="ingr-quality-cat">Category: <strong>{{system.ingredient_type}}</strong></span>
    </div>

    {{!-- Description --}}
    <div class="oi-content-area">
      <div class="editor-container">
        <div class="editor-header"><i class="fas fa-quill"></i> Description</div>
        {{editor system.description target="system.description" rollData=rollData button=true owner=owner editable=editable}}
      </div>
    </div>

  </div>
</form>
